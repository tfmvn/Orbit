# @orbit/shared

Types and constants shared between `apps/web` and any future Orbit client
(CLI, desktop shell, etc.). Contains no business logic — if you find
yourself adding a function with real behavior here, it likely belongs in a
more specific package instead.
